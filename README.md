# S-RIM 마법사 Plus

`index.html`은 엑셀 `S-RIM 마법사 Plus_V23.xlsx`의 핵심 RIM 계산식을 웹 계산기로 옮긴 파일입니다.

매일 최신 기준값은 GitHub Actions가 `scripts/update_market_data.py`를 실행해 `data/latest.json`을 갱신합니다. 기본 watchlist는 `data/watchlist.json`에서 관리합니다.
